<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Surface Area -->
    <com.dlazaro66.qrcodereaderview.QRCodeReaderView
        android:id="@+id/qrdecoderview"
        android:layout_width="288dp"
        android:layout_height="288dp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/squaretrack"
        android:layout_alignTop="@+id/qrdecoderview"
        android:layout_alignStart="@+id/qrdecoderview"
        android:layout_alignBottom="@+id/qrdecoderview"
        android:layout_alignEnd="@+id/qrdecoderview">

    </ImageView>

    <!-- Torch Button -->
    <ImageButton
        android:id="@+id/ibtn_torch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:layout_marginStart="20dp"
        android:layout_marginBottom="20dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:src="@drawable/ic_torch"
        android:background="@drawable/bg_rounded_orange_red"/>

</RelativeLayout>